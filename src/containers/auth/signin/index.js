import React from "react";
import { useForm } from "react-hook-form";

const Signin = () => {
  const {
    handleSubmit,
    register,
    formState: { errors },
  } = useForm();
  return <div></div>;
};

export default Signin;
